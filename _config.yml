title: "Zen UI - A minimal interface for e-readers"

include:
  - CONTRIBUTING.md

defaults:
  - scope:
      path: ""
    values:
      layout: default

remote_theme: jekyll/minima

plugins:
  - jekyll-remote-theme

minima:
  skin: auto
